# SDDE overlay (v0)

This folder is the “handle” SDDE OS uses to govern this repo.

In v0 it is documentation-only. In v1 we add:
- `sdde/contracts/*` (rigor, tradeoffs policy, stack plan)
- `sdde/blueprint/*` (schema-locked intake + slices)
- generated reports under `sdde/dist/*`

Until then, keep the web app deployable and stable.
