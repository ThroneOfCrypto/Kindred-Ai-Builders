# Periodic (Deterministic Core)

This repository embeds the **Periodic Tables** that define the contract-space SPEL reasons about.

In this repo:

- Tables live at `periodic/v1/**`
- Contract verification is performed by `npm run periodic:verify`
- Full reports are emitted to `dist/periodic_contracts_*.report.json` during builds/tests
